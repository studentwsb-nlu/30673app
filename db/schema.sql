CREATE TABLE IF NOT EXISTS gold_pln (
  date DATE PRIMARY KEY,
  value NUMERIC
);
CREATE TABLE IF NOT EXISTS gold_realestate (
  date DATE PRIMARY KEY,
  value NUMERIC
);
CREATE TABLE IF NOT EXISTS wig_realestate (
  date DATE PRIMARY KEY,
  value NUMERIC
);